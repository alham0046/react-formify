## ⚠️ Tailwind Required

These components are styled using Tailwind CSS utility classes.

Please ensure Tailwind is installed in your project.

### Install Tailwind:

```bash
npm install -D tailwindcss postcss autoprefixer
npx tailwindcss init
